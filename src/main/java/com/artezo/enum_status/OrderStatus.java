package com.artezo.enum_status;

public enum OrderStatus {
    PENDING,
    PLACED,
    SHIPPED,
    DELIVERED,
    CANCELLED,
    RETURN_REQUESTED,
    RETURNED,
    EXCHANGE_REQUESTED,
    EXCHANGED,
    FAILED
}